set(SOURCES
    Clause.cpp
    CNF.cpp
    DPLL.cpp
)

add_library(satFishLib ${SOURCES})
target_include_directories(satFishLib PUBLIC ../include)
target_compile_features(satFishLib PUBLIC cxx_std_22)


